# API ビヘイビア: <領域名>

<!-- ファイル名は api-<name>.md（kebab-case）とする -->

## プレフィクス

`<PREFIX>`（この領域のビヘイビア ID は `<PREFIX>-N`）

## エンドポイント一覧

| Method | Path | 概要 | 認証 |
|--------|------|------|------|
| GET | `/example` | ... | 必要 / 不要 |

## ビヘイビア一覧

<!-- 前提 = リクエスト条件、操作 = 呼び出し、期待 = レスポンス・副作用。曖昧語は使わない -->

| ID | 優先度 | ステータス | 振る舞い |
|----|--------|-----------|---------|
| <PREFIX>-1 | Must | 確定 | **前提**: … <br>**操作**: `GET /example` … <br>**期待**: `200` で … を返す |

## エラーハンドリング

<!-- エラーケースにもビヘイビア ID を振る（番号はファイル内通し） -->

| ID | 優先度 | ステータス | 振る舞い |
|----|--------|-----------|---------|
| <PREFIX>-2 | Must | 確定 | **前提**: <不正な入力条件> <br>**操作**: `GET /example` … <br>**期待**: `400` + `{"code": "...", "message": "..."}` を返す |

## レスポンススキーマ

<!-- 主要レスポンスの JSON 形。確定していない場合はステータス「検討中」の項目として扱う -->

```json
{
  "example": "..."
}
```

## 関連 PoC

- PoC-X: <この領域のビヘイビアの根拠となる検証結果>
